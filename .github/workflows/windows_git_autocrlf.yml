on:
  push:
jobs:
  build:
    runs-on: "windows-latest"
    name: Get Windows Config
    steps:
      - name: Global
        run: git config -l --global
        shell: cmd
      - name: System
        run: git config -l --system
        shell: cmd